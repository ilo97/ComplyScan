# 🔐 ComplyScan Pro — Environment Configuration

# === API Keys ===
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxx    # Claude AI for GDPR/SOC2/CCPA analysis

# === Stripe Billing ===
STRIPE_SECRET_KEY=sk_test_xxxxxxxxxxxx   # Stripe secret key
STRIPE_WEBHOOK_SECRET=whsec_xxxxxxxxxxx  # Stripe webhook signing secret

# Stripe Price IDs (create these in Stripe Dashboard)
STRIPE_PRICE_STARTER=price_xxxxxxxxxxxx  # $99/mo — 1 domain, weekly scan
STRIPE_PRICE_GROWTH=price_xxxxxxxxxxxx   # $299/mo — 5 domains, daily scan
STRIPE_PRICE_ENTERPRISE=price_xxxxxxx    # $999/mo — unlimited, API, white-label

# === Database ===
DATABASE_URL=sqlite:///./complyscan.db   # SQLite for dev. Use PostgreSQL in production

# === Security ===
SECRET_KEY=change-this-to-a-random-secret-key   # JWT signing key (min 32 chars)
ADMIN_SECRET_KEY=change-this-too                # Admin panel access key
ENVIRONMENT=development                          # "development" or "production"
CORS_ORIGINS=http://localhost:8002               # Comma-separated allowed origins

# === Rate Limiting ===
RATE_LIMIT_PER_MINUTE=60                         # Max requests per minute per IP
RATE_LIMIT_LOGIN=10                              # Max login attempts per minute

# === Email (Optional) ===
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=apikey
SMTP_PASSWORD=SG.xxxxxxxxxxxx
EMAIL_FROM=noreply@complyscan.com
